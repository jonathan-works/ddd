@model IEnumerable<JWorks.UI.Site.ViewModel.UserViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NmName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NmPassword)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DtSystem)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NmName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NmPassword)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DtSystem)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
